<Window x:Class="DupSweep.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:DupSweep.App.ViewModels"
        xmlns:views="clr-namespace:DupSweep.App.Views"
        mc:Ignorable="d"
        Title="DupSweep - Duplicate File Finder"
        Height="700" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ScanViewModel}">
            <views:ScanView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ResultsViewModel}">
            <views:ResultsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>

    <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
        <materialDesign:DrawerHost.LeftDrawerContent>
            <DockPanel MinWidth="220">
                <StackPanel DockPanel.Dock="Top" Margin="16">
                    <TextBlock Text="DupSweep"
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                               Foreground="{DynamicResource PrimaryHueMidBrush}" />
                    <TextBlock Text="Duplicate File Finder"
                               Style="{StaticResource MaterialDesignCaptionTextBlock}"
                               Opacity="0.6" />
                </StackPanel>

                <Separator DockPanel.Dock="Top" />

                <ListBox DockPanel.Dock="Top"
                         Margin="0,8"
                         SelectedIndex="{Binding SelectedNavIndex}"
                         Style="{StaticResource MaterialDesignNavigationListBox}">
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Home" VerticalAlignment="Center" Margin="0,0,16,0" />
                            <TextBlock Text="Home" VerticalAlignment="Center" />
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" Margin="0,0,16,0" />
                            <TextBlock Text="Scan" VerticalAlignment="Center" />
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileMultiple" VerticalAlignment="Center" Margin="0,0,16,0" />
                            <TextBlock Text="Results" VerticalAlignment="Center" />
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cog" VerticalAlignment="Center" Margin="0,0,16,0" />
                            <TextBlock Text="Settings" VerticalAlignment="Center" />
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>

                <StackPanel DockPanel.Dock="Bottom" Margin="16" VerticalAlignment="Bottom">
                    <TextBlock Text="Version 1.0.0"
                               Style="{StaticResource MaterialDesignCaptionTextBlock}"
                               Opacity="0.5"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </DockPanel>
        </materialDesign:DrawerHost.LeftDrawerContent>

        <DockPanel>
            <materialDesign:ColorZone DockPanel.Dock="Top"
                                      Mode="PrimaryMid"
                                      Padding="16"
                                      materialDesign:ElevationAssist.Elevation="Dp4">
                <DockPanel>
                    <ToggleButton x:Name="MenuToggleButton"
                                  DockPanel.Dock="Left"
                                  Style="{StaticResource MaterialDesignHamburgerToggleButton}"
                                  IsChecked="False" />

                    <TextBlock DockPanel.Dock="Left"
                               Text="{Binding Title}"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               VerticalAlignment="Center"
                               Margin="16,0,0,0" />

                    <materialDesign:PopupBox DockPanel.Dock="Right"
                                             PlacementMode="BottomAndAlignRightEdges"
                                             StaysOpen="False">
                        <StackPanel>
                            <Button Content="About" />
                            <Button Content="Help" />
                            <Separator />
                            <Button Content="Exit" Click="ExitButton_Click" />
                        </StackPanel>
                    </materialDesign:PopupBox>
                </DockPanel>
            </materialDesign:ColorZone>

            <ContentControl Content="{Binding CurrentView}"
                            Margin="16" />
        </DockPanel>
    </materialDesign:DrawerHost>
</Window>
