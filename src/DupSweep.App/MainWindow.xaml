<Window x:Class="DupSweep.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:DupSweep.App.ViewModels"
        xmlns:views="clr-namespace:DupSweep.App.Views"
        xmlns:controls="clr-namespace:DupSweep.App.Controls"
        mc:Ignorable="d"
        Title="DupSweep"
        Height="800" Width="1280"
        MinHeight="700" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ScanViewModel}">
            <views:ScanView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ResultsViewModel}">
            <views:ResultsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:FolderTreeViewModel}">
            <controls:FolderTreeView />
        </DataTemplate>

        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#B0BEC5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,14" />
            <Setter Property="Margin" Value="8,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A3A5F" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="12"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20"
                              ShadowDepth="0"
                              Color="#000000"
                              Opacity="0.15" />
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0"
                    Background="{StaticResource SidebarGradientBrush}"
                    CornerRadius="12,0,0,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Intentionally empty: Windows-style controls are in the top-right bar -->

                    <!-- Logo Section -->
                    <StackPanel Grid.Row="1" Margin="24,16,24,32">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="44" Height="44"
                                    CornerRadius="10"
                                    Background="{StaticResource AccentBrush}">
                                <materialDesign:PackIcon Kind="ContentDuplicate"
                                                         Width="26" Height="26"
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="DupSweep"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="White" />
                                <TextBlock Text="중복 파일 찾기"
                                           FontSize="11"
                                           Foreground="#78909C"
                                           Margin="0,-2,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- Navigation -->
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="메인 메뉴"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="#546E7A"
                                   Margin="24,0,0,12" />

                        <RadioButton x:Name="HomeNav"
                                     Style="{StaticResource NavButton}"
                                     IsChecked="True"
                                     Checked="NavItem_Checked"
                                     Tag="0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Home" Width="22" Height="22" VerticalAlignment="Center" />
                                <TextBlock Text="홈" FontSize="14" FontWeight="Medium" Margin="14,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>

                        <RadioButton x:Name="ScanNav"
                                     Style="{StaticResource NavButton}"
                                     Checked="NavItem_Checked"
                                     Tag="1">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Magnify" Width="22" Height="22" VerticalAlignment="Center" />
                                <TextBlock Text="스캔" FontSize="14" FontWeight="Medium" Margin="14,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>

                        <RadioButton x:Name="ResultsNav"
                                     Style="{StaticResource NavButton}"
                                     Checked="NavItem_Checked"
                                     Tag="2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileMultiple" Width="22" Height="22" VerticalAlignment="Center" />
                                <TextBlock Text="결과" FontSize="14" FontWeight="Medium" Margin="14,0,0,0" VerticalAlignment="Center" />
                                <Border x:Name="ResultsBadge"
                                        Style="{StaticResource Badge}"
                                        Margin="12,0,0,0"
                                        Visibility="Collapsed">
                                    <TextBlock x:Name="ResultsBadgeText"
                                               Text="0"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="White" />
                                </Border>
                            </StackPanel>
                        </RadioButton>

                        <Separator Margin="24,20" Background="#263850" />

                        <TextBlock Text="도구"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="#546E7A"
                                   Margin="24,0,0,12" />

                        <RadioButton x:Name="FolderTreeNav"
                                     Style="{StaticResource NavButton}"
                                     Checked="NavItem_Checked"
                                     Tag="4">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileTree" Width="22" Height="22" VerticalAlignment="Center" />
                                <TextBlock Text="폴더 트리" FontSize="14" FontWeight="Medium" Margin="14,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>

                        <Separator Margin="24,20" Background="#263850" />

                        <TextBlock Text="설정"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="#546E7A"
                                   Margin="24,0,0,12" />

                        <RadioButton x:Name="SettingsNav"
                                     Style="{StaticResource NavButton}"
                                     Checked="NavItem_Checked"
                                     Tag="3">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CogOutline" Width="22" Height="22" VerticalAlignment="Center" />
                                <TextBlock Text="환경설정" FontSize="14" FontWeight="Medium" Margin="14,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>

                    <!-- Footer -->
                    <Border Grid.Row="3"
                            Background="#0F1E33"
                            CornerRadius="0,0,0,12"
                            Padding="20,16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="InformationOutline"
                                                         Width="16" Height="16"
                                                         Foreground="#546E7A"
                                                         VerticalAlignment="Center" />
                                <TextBlock Text="Version 2.0.0"
                                           FontSize="12"
                                           Foreground="#546E7A"
                                           Margin="8,0,0,0"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Top Bar -->
                <Border Grid.Row="0"
                        Background="{StaticResource SurfaceBrush}"
                        Padding="24,16"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Page Title -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}"
                                       Style="{StaticResource HeadlineMedium}" />
                            <TextBlock x:Name="PageSubtitle"
                                       Text="중복 파일을 검색할 폴더를 선택하세요"
                                       Style="{StaticResource Caption}"
                                       Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Actions -->
                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <!-- Quick Actions -->
                            <Button Style="{StaticResource IconButton}"
                                    ToolTip="도움말 (F1)"
                                    Margin="0,0,4,0">
                                <materialDesign:PackIcon Kind="HelpCircleOutline" Width="22" Height="22" />
                            </Button>
                            <Button Style="{StaticResource IconButton}"
                                    ToolTip="알림"
                                    Margin="0,0,4,0">
                                <materialDesign:PackIcon Kind="BellOutline" Width="22" Height="22" />
                            </Button>

                            <Separator Width="1" Height="28" Background="{StaticResource BorderBrush}" Margin="8,0" />

                            <!-- Window Controls -->
                            <Button Style="{StaticResource IconButton}"
                                    Click="MinimizeButton_Click"
                                    ToolTip="최소화">
                                <materialDesign:PackIcon Kind="WindowMinimize" Width="18" Height="18" />
                            </Button>
                            <Button Style="{StaticResource IconButton}"
                                    Click="MaximizeButton_Click"
                                    ToolTip="최대화">
                                <materialDesign:PackIcon Kind="WindowMaximize" Width="18" Height="18" />
                            </Button>
                            <Button Style="{StaticResource IconButton}"
                                    Click="CloseButton_Click"
                                    ToolTip="닫기">
                                <materialDesign:PackIcon Kind="Close" Width="18" Height="18" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content -->
                <Border Grid.Row="1"
                        Background="{StaticResource BackgroundBrush}"
                        CornerRadius="0,0,12,0">
                    <Grid>
                        <ContentControl Content="{Binding CurrentView}"
                                        Margin="24,16,24,24" />

                        <!-- Notification Host - Toast notifications overlay -->
                        <controls:NotificationHost VerticalAlignment="Bottom"
                                                   HorizontalAlignment="Right"
                                                   Margin="0,0,8,8" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
