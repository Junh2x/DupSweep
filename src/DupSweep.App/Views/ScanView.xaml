<UserControl x:Class="DupSweep.App.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:DupSweep.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewmodels:ScanViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <StackPanel Grid.Row="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="500">

            <!-- Progress Ring -->
            <Grid HorizontalAlignment="Center" Margin="0,0,0,32">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="{Binding Progress}"
                             Width="200" Height="200"
                             IsIndeterminate="False" />
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Progress, StringFormat={}{0:0}%}"
                               Style="{StaticResource MaterialDesignHeadline3TextBlock}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding StatusMessage}"
                               Style="{StaticResource MaterialDesignCaptionTextBlock}"
                               HorizontalAlignment="Center"
                               Opacity="0.6" />
                </StackPanel>
            </Grid>

            <!-- Statistics -->
            <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp2" Padding="24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding ScannedFiles}"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Scan.ProcessedFiles}"
                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                   HorizontalAlignment="Center"
                                   Opacity="0.6" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding DuplicateGroups}"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource SecondaryHueMidBrush}" />
                        <TextBlock Text="{DynamicResource Scan.DuplicateGroups}"
                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                   HorizontalAlignment="Center"
                                   Opacity="0.6" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding FormattedPotentialSavings}"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}" />
                        <TextBlock Text="{DynamicResource Scan.SavableSpace}"
                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                   HorizontalAlignment="Center"
                                   Opacity="0.6" />
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Current File -->
            <TextBlock Text="{Binding CurrentFile}"
                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                       TextTrimming="CharacterEllipsis"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0"
                       Opacity="0.6"
                       MaxWidth="500" />
        </StackPanel>

        <!-- Control Buttons (only visible when scanning) -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,32,0,0"
                    Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}">
            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding PauseScanCommand}"
                    Margin="0,0,16,0"
                    Padding="24,8">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="{Binding IsPaused, Converter={StaticResource BoolToPausePlayIconConverter}, FallbackValue=Pause}"
                                             VerticalAlignment="Center"
                                             Margin="0,0,8,0" />
                    <TextBlock Text="{Binding IsPaused, Converter={StaticResource BoolToPauseResumeTextConverter}, FallbackValue=Pause}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button Content="{DynamicResource Scan.Cancel}"
                    Style="{StaticResource MaterialDesignOutlinedSecondaryButton}"
                    Command="{Binding CancelScanCommand}"
                    Padding="24,8">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Close" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
